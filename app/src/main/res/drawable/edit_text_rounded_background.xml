<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#10000000" /> <!-- Very light black (10% opacity) -->
    <corners android:radius="10dp" />
    <stroke
        android:width="1dp"
        android:color="#20000000" /> <!-- Slightly darker border (20% opacity) -->
    <padding
        android:bottom="8dp"
        android:left="12dp"
        android:right="12dp"
        android:top="8dp" />
</shape>
